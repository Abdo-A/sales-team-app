import React from 'react';
import { View, Text } from 'react-native-ui-lib';

const DCownersRegisterInputs = () => {
  const text = 'DC owners';
  return (
    <View>
      <Text>{text}</Text>
    </View>
  );
};

export default DCownersRegisterInputs;
