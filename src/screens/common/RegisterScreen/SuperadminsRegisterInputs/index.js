import React from 'react';
import { View, Text } from 'react-native-ui-lib';

const SuperadminsRegisterInputs = () => {
  const text = 'Superadmins';
  return (
    <View>
      <Text>{text}</Text>
    </View>
  );
};

export default SuperadminsRegisterInputs;
