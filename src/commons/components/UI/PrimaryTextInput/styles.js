import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '80%', margin: 10, paddingHorizontal: 10, paddingBottom: 5, paddingTop: 3, alignSelf: 'center', borderRadius: 4,
  },
});

export default styles;
